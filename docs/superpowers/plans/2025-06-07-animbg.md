# 动画背景特效系统 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 抓取 htmlhub.org `/library` 全部全屏特效作为动画背景，引入三种互斥背景源参数（`--bg-img` / `--bg-video` / `--bganim`），并把 8 个 preset 的背景逻辑收敛到一个共享组件。

**Architecture:** Python 脚本抓取特效 HTML 入库到 `src/animbg/<label>/`；新增共享 React 组件 `BackgroundLayer` 统一按优先级渲染 video/img/anim/渐变；CLI/render 层解析互斥背景参数并把数据注入 props。**实现第一步是 POC**：验证 `<IFrame srcDoc>` 动画能在 headless Remotion 输出视频里真正逐帧动起来，再大规模推进。

**Tech Stack:** Remotion 4.0.417（`IFrame` / `OffthreadVideo` / `Img`）、React + TypeScript、Node CLI（`cli.mjs` / `render.mjs`）、Python 3（`requests` + `beautifulsoup4`）。

**注意：本仓库没有 JS 测试框架**（package.json 仅 `@types/react` + `typescript`）。前端验证手段是 `npx tsc --noEmit` 类型检查 + `remotion still` 抽帧人工核验。Python 脚本用一个独立的可运行检查脚本验证。背景特效为第三方代码，manifest 保留来源 URL 注明出处。

---

## 文件结构

- **Create** `src/scripts/fetch_animbg.py` — 抓取脚本（库收集 → 详情页提取 → 写 HTML + manifest）
- **Create** `src/scripts/requirements.txt` — Python 依赖
- **Create** `src/animbg/<label>/index.html`（×~71，脚本生成）+ `src/animbg/manifest.json`（脚本生成）
- **Create** `src/preset/BackgroundLayer.tsx` — 共享背景组件
- **Modify** `src/types.ts` — `MVInputProps` 新增 `backgroundVideo` / `backgroundAnimHtml`
- **Modify** 8 个 preset 的 Composition 文件 — 改用 `<BackgroundLayer>`
  - `src/preset/orig/AudioVisualization.tsx`
  - `src/preset/no2/Composition.tsx`
  - `src/preset/apple/Composition.tsx`
  - `src/preset/ktv/Composition.tsx`
  - `src/preset/neon/Composition.tsx`
  - `src/preset/cinema/Composition.tsx`
  - `src/preset/bounce/Composition.tsx`
  - `src/preset/typewriter/Composition.tsx`
- **Modify** `src/render.mjs` — 互斥背景参数、复制视频、读取 animbg HTML、注入 props
- **Modify** `src/cli.mjs` — 转发新参数、`--help` 文档
- **Modify** `USAGE.md` — 参数表 + 示例

---

## Task 0: POC — 验证 iframe 动画能在输出视频里逐帧动起来

**这是最高风险点，必须先做。** 如果失败，停止并报告，讨论回退方案（主上下文注入 JS）。

**Files:**
- Create (临时): `src/poc/PocRoot.tsx`
- Create (临时): `src/poc/effect.html`, `src/poc/props.json`

- [ ] **Step 1: 抓一个 Canvas 特效作为 POC 素材**

Run:
```bash
cd /Users/dfbb/Sites/mtv/mutv/src
python3 - <<'PY'
import requests
from bs4 import BeautifulSoup
import os
r=requests.get("https://htmlhub.org/library/particles-systems/cosmic",
               headers={"User-Agent":"Mozilla/5.0"}, timeout=30)
soup=BeautifulSoup(r.text,"html.parser")
doc=soup.select_one('pre[data-content="all"]').get_text()
os.makedirs("poc",exist_ok=True)
open("poc/effect.html","w",encoding="utf-8").write(doc)
print("wrote poc/effect.html", len(doc), "bytes; has canvas:", "<canvas" in doc)
PY
```
Expected: `wrote poc/effect.html ~6900 bytes; has canvas: True`

- [ ] **Step 2: 写 POC 组件（iframe 背景 + 帧号叠加）**

Create `src/poc/PocRoot.tsx`:
```tsx
import React from 'react';
import {registerRoot} from 'remotion';
import {Composition, AbsoluteFill, IFrame, useCurrentFrame} from 'remotion';

const Poc: React.FC<{html: string}> = ({html}) => {
  const frame = useCurrentFrame();
  return (
    <AbsoluteFill style={{backgroundColor: 'black'}}>
      <IFrame srcDoc={html} style={{width: '100%', height: '100%', border: 'none'}} />
      <AbsoluteFill style={{justifyContent: 'flex-start', alignItems: 'flex-start'}}>
        <div style={{color: 'red', fontSize: 80, fontWeight: 900, padding: 20}}>frame {frame}</div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

const Root: React.FC = () => (
  <Composition id="Poc" component={Poc} durationInFrames={90} fps={30} width={1280} height={720} defaultProps={{html: ''}} />
);

registerRoot(Root);
```

- [ ] **Step 3: 渲染 POC 并抽三帧**

Run:
```bash
cd /Users/dfbb/Sites/mtv/mutv/src
HSHELL="node_modules/.remotion/chrome-headless-shell/mac-arm64/chrome-headless-shell-mac-arm64/chrome-headless-shell"
node -e "const fs=require('fs');const h=fs.readFileSync('poc/effect.html','utf-8');fs.writeFileSync('poc/props.json',JSON.stringify({html:h}))"
for F in 5 40 80; do
  npx remotion still poc/PocRoot.tsx Poc "poc/frame_$F.png" --props=poc/props.json --frame=$F --log=error --browser-executable="$HSHELL"
done
ls -la poc/frame_*.png
```
Expected: 三个 PNG（`frame_5.png` / `frame_40.png` / `frame_80.png`）写入成功。

- [ ] **Step 4: 人工核验背景是否在动**

用 Read 工具查看 `poc/frame_5.png`、`poc/frame_40.png`、`poc/frame_80.png`。判定标准：
- 三帧右上角 `frame N` 数字不同（确认 Remotion 帧推进正常）。
- 三帧的 **iframe 背景特效画面互不相同**（粒子/星空位置变化）→ **iframe 动画逐帧生效，POC 通过**。
- 若三帧背景完全一致（静止首帧）→ **POC 失败**。

- [ ] **Step 5: 记录结论并清理**

如果通过：在对话里说明"POC 通过，iframe 动画逐帧生效"，删除 POC 临时文件后继续 Task 1。
如果失败：**停止**，报告"iframe 背景在输出视频中静止"，等待用户决定回退方案。

Run（仅通过时）:
```bash
cd /Users/dfbb/Sites/mtv/mutv/src && rm -rf poc
```

- [ ] **Step 6: 不提交**（POC 是一次性验证，无需提交）

---

## Task 1: 扩展 MVInputProps 类型

**Files:**
- Modify: `src/types.ts`

- [ ] **Step 1: 新增两个背景字段到 interface**

在 `src/types.ts` 的 `MVInputProps` 里，把现有 `backgroundImage` 那段注释+字段替换为下面三行（保留 backgroundImage，新增两个）：
```ts
  /** Background image filename (relative to public/ or absolute URL). */
  backgroundImage: string;
  /** Background video filename (relative to public/ or absolute URL). Takes priority over image/anim. */
  backgroundVideo: string;
  /** Full self-contained HTML of an animated background effect (from animbg/<label>/index.html). */
  backgroundAnimHtml: string;
```

- [ ] **Step 2: 给 defaultProps 补默认值**

在 `src/types.ts` 的 `defaultProps` 对象里，`backgroundImage: '',` 之后新增：
```ts
  backgroundVideo: '',
  backgroundAnimHtml: '',
```

- [ ] **Step 3: 类型检查**

Run: `cd /Users/dfbb/Sites/mtv/mutv/src && npx tsc --noEmit`
Expected: 退出码 0（无错误）。

- [ ] **Step 4: Commit**

```bash
cd /Users/dfbb/Sites/mtv/mutv
git add src/types.ts
git commit -m "feat(types): add backgroundVideo and backgroundAnimHtml props"
```

---

## Task 2: 共享 BackgroundLayer 组件

**Files:**
- Create: `src/preset/BackgroundLayer.tsx`

- [ ] **Step 1: 写组件**

Create `src/preset/BackgroundLayer.tsx`:
```tsx
import React from 'react';
import {AbsoluteFill, Img, IFrame, OffthreadVideo, staticFile} from 'remotion';

/**
 * Shared background layer for all presets. Renders exactly one source by
 * priority: video > image > animated-html > fallback gradient.
 *
 * - backgroundVideo:    looping OffthreadVideo, cover
 * - backgroundImage:    Img, cover (+ optional dark overlay)
 * - backgroundAnimHtml: full self-contained HTML in an <IFrame srcDoc>
 * - fallbackGradient:   CSS background value supplied by each preset so its
 *                       own visual style is preserved when no source is given
 */
export const BackgroundLayer: React.FC<{
  backgroundVideo?: string;
  backgroundImage?: string;
  backgroundAnimHtml?: string;
  fallbackGradient: string;
  /** Optional dark overlay over video/image for text readability (e.g. 'rgba(0,0,0,0.45)'). */
  overlay?: string;
}> = ({backgroundVideo, backgroundImage, backgroundAnimHtml, fallbackGradient, overlay}) => {
  const toSrc = (s: string) => (s.startsWith('http') ? s : staticFile(s));

  if (backgroundVideo) {
    return (
      <>
        <AbsoluteFill>
          <OffthreadVideo
            src={toSrc(backgroundVideo)}
            muted
            loop
            style={{width: '100%', height: '100%', objectFit: 'cover'}}
          />
        </AbsoluteFill>
        {overlay ? <AbsoluteFill style={{background: overlay}} /> : null}
      </>
    );
  }

  if (backgroundImage) {
    return (
      <>
        <AbsoluteFill>
          <Img src={toSrc(backgroundImage)} style={{width: '100%', height: '100%', objectFit: 'cover'}} />
        </AbsoluteFill>
        {overlay ? <AbsoluteFill style={{background: overlay}} /> : null}
      </>
    );
  }

  if (backgroundAnimHtml) {
    return (
      <AbsoluteFill>
        <IFrame srcDoc={backgroundAnimHtml} style={{width: '100%', height: '100%', border: 'none'}} />
      </AbsoluteFill>
    );
  }

  return <AbsoluteFill style={{background: fallbackGradient}} />;
};
```

- [ ] **Step 2: 类型检查**

Run: `cd /Users/dfbb/Sites/mtv/mutv/src && npx tsc --noEmit`
Expected: 退出码 0。

- [ ] **Step 3: Commit**

```bash
cd /Users/dfbb/Sites/mtv/mutv
git add src/preset/BackgroundLayer.tsx
git commit -m "feat(preset): add shared BackgroundLayer (video>image>anim>gradient)"
```

---

## Task 3: Python 抓取脚本

**Files:**
- Create: `src/scripts/requirements.txt`
- Create: `src/scripts/fetch_animbg.py`

- [ ] **Step 1: 写 requirements.txt**

Create `src/scripts/requirements.txt`:
```
requests>=2.31
beautifulsoup4>=4.12
```

- [ ] **Step 2: 写抓取脚本**

Create `src/scripts/fetch_animbg.py`:
```python
#!/usr/bin/env python3
"""Fetch all fullscreen effects from htmlhub.org /library into src/animbg/.

For each effect detail page, the full self-contained HTML lives in
<pre data-content="all"><code>…escaped…</code></pre>; BeautifulSoup's
get_text() returns it unescaped and runnable.

Outputs:
  src/animbg/<label>/index.html   one per effect
  src/animbg/manifest.json        [{label,name,category,tech,sourceUrl,fetchedAt}]

Idempotent: re-running overwrites. Failures are logged and skipped, not fatal.
"""
import json
import os
import re
import sys
import time
from datetime import datetime, timezone

import requests
from bs4 import BeautifulSoup

BASE = "https://htmlhub.org"
LIBRARY = BASE + "/library"
HEADERS = {"User-Agent": "Mozilla/5.0 (compatible; mtv-animbg-fetch/1.0)"}
HERE = os.path.dirname(os.path.abspath(__file__))
OUT_DIR = os.path.normpath(os.path.join(HERE, "..", "animbg"))
DELAY = 0.5  # polite spacing between requests (seconds)


def get(url):
    resp = requests.get(url, headers=HEADERS, timeout=30)
    resp.raise_for_status()
    return resp.text


def collect_detail_urls():
    """Return sorted unique list of /library/<cat>/<slug> absolute URLs."""
    html = get(LIBRARY)
    paths = set(re.findall(r"/library/[a-z0-9-]+/[a-z0-9-]+", html))
    # Also crawl each category page to catch effects not linked on /library.
    cats = set(re.findall(r"/library/([a-z0-9-]+)(?:\"|/)", html))
    for cat in sorted(cats):
        try:
            chtml = get(LIBRARY + "/" + cat)
            paths.update(re.findall(r"/library/" + re.escape(cat) + r"/[a-z0-9-]+", chtml))
            time.sleep(DELAY)
        except Exception as e:
            print(f"  ! category {cat} failed: {e}", file=sys.stderr)
    return sorted(BASE + p for p in paths)


def parse_detail(url):
    """Return (label, name, category, tech, html) or None on failure."""
    html = get(url)
    soup = BeautifulSoup(html, "html.parser")
    pre = soup.select_one('pre[data-content="all"]')
    if pre is None:
        return None
    doc = pre.get_text()
    if "<!DOCTYPE" not in doc and "<html" not in doc:
        return None
    m = re.search(r"/library/([a-z0-9-]+)/([a-z0-9-]+)", url)
    category, slug = (m.group(1), m.group(2)) if m else ("misc", url.rsplit("/", 1)[-1])
    label = slug
    h1 = soup.find("h1")
    name = h1.get_text(strip=True) if h1 else slug
    lower = doc.lower()
    tech = "webgl" if ("three" in lower or "webgl" in lower) else ("svg" if "<svg" in lower else "canvas")
    return label, name, category, tech, doc


def main():
    os.makedirs(OUT_DIR, exist_ok=True)
    urls = collect_detail_urls()
    print(f"Found {len(urls)} detail URLs")
    manifest = []
    seen_labels = {}
    ok = fail = 0
    for url in urls:
        try:
            parsed = parse_detail(url)
            if parsed is None:
                print(f"  ! no source block: {url}", file=sys.stderr)
                fail += 1
                continue
            label, name, category, tech, doc = parsed
            # de-dup labels across categories
            if label in seen_labels and seen_labels[label] != category:
                label = f"{category}-{label}"
            seen_labels[label] = category
            d = os.path.join(OUT_DIR, label)
            os.makedirs(d, exist_ok=True)
            with open(os.path.join(d, "index.html"), "w", encoding="utf-8") as f:
                f.write(doc)
            manifest.append({
                "label": label, "name": name, "category": category, "tech": tech,
                "sourceUrl": url, "fetchedAt": datetime.now(timezone.utc).isoformat(),
            })
            ok += 1
            print(f"  + {label} ({tech})")
            time.sleep(DELAY)
        except Exception as e:
            print(f"  ! {url} failed: {e}", file=sys.stderr)
            fail += 1
    manifest.sort(key=lambda x: x["label"])
    with open(os.path.join(OUT_DIR, "manifest.json"), "w", encoding="utf-8") as f:
        json.dump(manifest, f, ensure_ascii=False, indent=2)
    print(f"\nDone: {ok} ok, {fail} failed. manifest.json has {len(manifest)} entries.")
    if ok == 0:
        sys.exit(1)


if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 运行抓取脚本**

Run:
```bash
cd /Users/dfbb/Sites/mtv/mutv/src && python3 scripts/fetch_animbg.py
```
Expected: 打印 `Found N detail URLs`、逐条 `+ <label> (tech)`，结尾 `Done: M ok, K failed. manifest.json has M entries.`，M 应在 ~50–71 之间。

- [ ] **Step 4: 验证产物**

Run:
```bash
cd /Users/dfbb/Sites/mtv/mutv/src
node -e "const m=require('./animbg/manifest.json');console.log('entries:',m.length);console.log('tech breakdown:',m.reduce((a,x)=>{a[x.tech]=(a[x.tech]||0)+1;return a},{}));console.log('sample:',m[0])"
ls animbg | head
# pick first label, confirm its index.html is a full doc
node -e "const m=require('./animbg/manifest.json');const fs=require('fs');const h=fs.readFileSync('animbg/'+m[0].label+'/index.html','utf-8');console.log(m[0].label,'starts:',h.slice(0,20),'| len:',h.length)"
```
Expected: entries 数 > 40；每个 sample 有 label/name/category/tech/sourceUrl；index.html 以 `<!DOCTYPE html>` 开头。

- [ ] **Step 5: Commit（脚本 + 产物均入库）**

```bash
cd /Users/dfbb/Sites/mtv/mutv
git add src/scripts/fetch_animbg.py src/scripts/requirements.txt src/animbg
git commit -m "feat(animbg): add fetch script and scraped effect library"
```

---

## Task 4: 接入 BackgroundLayer 到 8 个 preset

每个 preset 的改造模式相同：
1. import `BackgroundLayer`
2. 在解构 props 里加 `backgroundVideo, backgroundAnimHtml`（`backgroundImage` 已有）
3. 删除 `bgSrc` 局部常量
4. 把原背景 JSX 块替换为 `<BackgroundLayer .../>`，`fallbackGradient` 用该 preset 原来的渐变值

每个 preset 改完后**统一类型检查**，最后一次性提交。

### 4a: no2 — `src/preset/no2/Composition.tsx`

- [ ] **Step 1: 改 import 行（第 1-5 行）**

把第 1 行和第 5 行之间的 import 区改为（新增第 6 行 BackgroundLayer import）：
```tsx
import {AbsoluteFill, Audio, staticFile} from 'remotion';
import {Bottom} from './Bottom';
import {fontSize} from './Dots';
import {Subtitles} from './Subtitles';
import {MVInputProps} from '../../types';
import {BackgroundLayer} from '../BackgroundLayer';
```
（注意：移除了不再使用的 `Img`。）

- [ ] **Step 2: 改解构 + 删除 bgSrc（第 7-21 行）**

把 `export const MyComposition...` 到 `: '';` 整段替换为：
```tsx
export const MyComposition: React.FC<MVInputProps> = ({
	audioFileName,
	backgroundImage,
	backgroundVideo,
	backgroundAnimHtml,
	lyrics,
	lyricOffset,
	title,
}) => {
	const audioSrc = audioFileName.startsWith('http')
		? audioFileName
		: staticFile(audioFileName);
```

- [ ] **Step 3: 替换背景 JSX（原第 32-39 行的 `{bgSrc ? ... : null}`）**

把那段替换为：
```tsx
			<BackgroundLayer
				backgroundVideo={backgroundVideo}
				backgroundImage={backgroundImage}
				backgroundAnimHtml={backgroundAnimHtml}
				fallbackGradient="black"
			/>
```

### 4b: ktv — `src/preset/ktv/Composition.tsx`

- [ ] **Step 4: 改 import + 解构 + 替换背景块**

在文件顶部 import 区加：
```tsx
import {BackgroundLayer} from '../BackgroundLayer';
```
在解构里 `backgroundImage,` 后加 `backgroundVideo,` 和 `backgroundAnimHtml,`。删除 `const bgSrc = ...` 那段。把 `{bgSrc ? (<AbsoluteFill><Img .../></AbsoluteFill>) : (<AbsoluteFill style={{background:'linear-gradient(180deg, #0a1030 0%, #05060f 60%, #03040a 100%)'}} />)}` 整块替换为：
```tsx
      <BackgroundLayer
        backgroundVideo={backgroundVideo}
        backgroundImage={backgroundImage}
        backgroundAnimHtml={backgroundAnimHtml}
        fallbackGradient="linear-gradient(180deg, #0a1030 0%, #05060f 60%, #03040a 100%)"
      />
```
如果文件中 `Img` 不再被使用，从 import 移除它。

### 4c: neon — `src/preset/neon/Composition.tsx`

- [ ] **Step 5: 改 import + 解构 + 替换背景块**

import 区加 `import {BackgroundLayer} from '../BackgroundLayer';`。解构 `backgroundImage,` 后加 `backgroundVideo,`、`backgroundAnimHtml,`。删除 `const bgSrc = ...`。把 `{bgSrc ? (<Img .../>) : (<AbsoluteFill style={{background: 'linear-gradient(135deg, #1a0033 0%, #0a0014 60%, #05000a 100%)'}} />)}` 替换为：
```tsx
      <BackgroundLayer
        backgroundVideo={backgroundVideo}
        backgroundImage={backgroundImage}
        backgroundAnimHtml={backgroundAnimHtml}
        fallbackGradient="linear-gradient(135deg, #1a0033 0%, #0a0014 60%, #05000a 100%)"
      />
```
若 `Img` 不再使用则从 import 移除。**保留** scanline / bottom-gradient 等其它叠加层不动。

### 4d: cinema — `src/preset/cinema/Composition.tsx`

- [ ] **Step 6: 改 import + 解构 + 替换背景块**

同上模式。替换 `{bgSrc ? (<Img .../>) : (<AbsoluteFill style={{background: 'radial-gradient(ellipse at center, #1a1a1a 0%, #000 100%)'}} />)}` 为：
```tsx
      <BackgroundLayer
        backgroundVideo={backgroundVideo}
        backgroundImage={backgroundImage}
        backgroundAnimHtml={backgroundAnimHtml}
        fallbackGradient="radial-gradient(ellipse at center, #1a1a1a 0%, #000 100%)"
      />
```
**保留** letterbox bars / vignette 叠加层不动。若 `Img` 不再使用则移除。

### 4e: bounce — `src/preset/bounce/Composition.tsx`

- [ ] **Step 7: 改 import + 解构 + 替换背景块**

替换 `{bgSrc ? (<Img .../>) : (<AbsoluteFill style={{background: 'linear-gradient(135deg, #2b1055 0%, #7597de 100%)'}} />)}` 为：
```tsx
      <BackgroundLayer
        backgroundVideo={backgroundVideo}
        backgroundImage={backgroundImage}
        backgroundAnimHtml={backgroundAnimHtml}
        fallbackGradient="linear-gradient(135deg, #2b1055 0%, #7597de 100%)"
      />
```
**保留** radial vignette 叠加层。若 `Img` 不再使用则移除。

### 4f: typewriter — `src/preset/typewriter/Composition.tsx`

- [ ] **Step 8: 改 import + 解构 + 替换背景块**

替换 `{bgSrc ? (<Img .../>) : (<AbsoluteFill style={{background: 'linear-gradient(180deg, #1a1a1a 0%, #0a0a0a 100%)'}} />)}` 为：
```tsx
      <BackgroundLayer
        backgroundVideo={backgroundVideo}
        backgroundImage={backgroundImage}
        backgroundAnimHtml={backgroundAnimHtml}
        fallbackGradient="linear-gradient(180deg, #1a1a1a 0%, #0a0a0a 100%)"
      />
```
若 `Img` 不再使用则移除。

### 4g: apple — `src/preset/apple/Composition.tsx`

- [ ] **Step 9: 改 import + 解构 + 替换背景块（带 overlay）**

apple 原背景块是 `{bgSrc ? (<Img/> + radial 暗化 overlay) : (<gradient/>)}`。import 区加 `import {BackgroundLayer} from '../BackgroundLayer';`，解构加 `backgroundVideo,`、`backgroundAnimHtml,`，删除 `bgSrc`。把整个背景块替换为：
```tsx
      <BackgroundLayer
        backgroundVideo={backgroundVideo}
        backgroundImage={backgroundImage}
        backgroundAnimHtml={backgroundAnimHtml}
        fallbackGradient="linear-gradient(135deg, hsl(260, 55%, 22%) 0%, hsl(320, 50%, 14%) 100%)"
        overlay="radial-gradient(ellipse at 30% 50%, rgba(0,0,0,0.25) 0%, rgba(0,0,0,0.6) 100%)"
      />
```
（注意：apple 原渐变含动态 `hue` 变量；这里固定为基准值 260/320，因为 BackgroundLayer 的 fallback 是静态字符串。动态 hue 漂移属于细节，简化为静态可接受。若 `Img` 不再使用则移除。）

### 4h: orig — `src/preset/orig/AudioVisualization.tsx`

- [ ] **Step 10: 改 import + 删除 bgSrc + 替换背景块（带 overlay）**

orig 原背景块（约第 82-112 行）是 `{bgSrc ? (<Img/> + rgba(0,0,0,0.45) overlay) : (<hsl gradient/>)}`，其后还有一个独立的 radial glow 层和 `<Audio>`。

import 区加 `import {BackgroundLayer} from '../BackgroundLayer';`。在解构 props 里 `backgroundImage,` 后加 `backgroundVideo,`、`backgroundAnimHtml,`。删除 `const bgSrc = ...` 那段。把 `{bgSrc ? (...) : (...)}` 整块（到 radial glow 层之前）替换为：
```tsx
      <BackgroundLayer
        backgroundVideo={backgroundVideo}
        backgroundImage={backgroundImage}
        backgroundAnimHtml={backgroundAnimHtml}
        fallbackGradient={`linear-gradient(135deg, hsl(${hue}, 80%, 12%) 0%, hsl(${hue + 80}, 70%, 8%) 100%)`}
        overlay="rgba(0, 0, 0, 0.45)"
      />
```
（`hue` 是组件内已有的局部变量，模板字符串里直接用。）**保留** radial glow 层、`<Audio>`、frequency bars 不动。若 `Img` 不再使用则从 import 移除。

- [ ] **Step 11: 统一类型检查**

Run: `cd /Users/dfbb/Sites/mtv/mutv/src && npx tsc --noEmit`
Expected: 退出码 0。若报 `Img` 未使用等错误，回到对应 preset 移除未用 import。

- [ ] **Step 12: 渲染抽帧验证三类背景源 + 渐变兜底**

Run（用已有的 cn-1.mp3；anim 用 manifest 第一个 label；逐项渲一帧）:
```bash
cd /Users/dfbb/Sites/mtv/mutv/src
HSHELL="node_modules/.remotion/chrome-headless-shell/mac-arm64/chrome-headless-shell-mac-arm64/chrome-headless-shell"
cp ../example/cn-1.mp3 public/ 2>/dev/null
LABEL=$(node -e "console.log(require('./animbg/manifest.json')[0].label)")
ANIMHTML=$(node -e "const fs=require('fs');const l=require('./animbg/manifest.json')[0].label;process.stdout.write(fs.readFileSync('animbg/'+l+'/index.html','utf-8'))")
# gradient fallback (no bg)
node -e "const fs=require('fs');fs.writeFileSync('/tmp/bg_grad.json',JSON.stringify({audioFileName:'cn-1.mp3',lyrics:[{start:0,end:5,text:'测试 test'}],title:'t',subtitle:'',creditText:'',durationInSeconds:8,lyricOffset:0,backgroundImage:'',backgroundVideo:'',backgroundAnimHtml:'',width:1280,height:720,fps:30})"
# anim fallback
node -e "const fs=require('fs');const l=require('./animbg/manifest.json')[0].label;const h=fs.readFileSync('animbg/'+l+'/index.html','utf-8');fs.writeFileSync('/tmp/bg_anim.json',JSON.stringify({audioFileName:'cn-1.mp3',lyrics:[{start:0,end:5,text:'测试 test'}],title:'t',subtitle:'',creditText:'',durationInSeconds:8,lyricOffset:0,backgroundImage:'',backgroundVideo:'',backgroundAnimHtml:h,width:1280,height:720,fps:30}))"
npx remotion still preset/ktv/index.ts MusicVideo /tmp/v_grad.png --props=/tmp/bg_grad.json --frame=60 --log=error --browser-executable="$HSHELL"
npx remotion still preset/ktv/index.ts MusicVideo /tmp/v_anim.png --props=/tmp/bg_anim.json --frame=60 --log=error --browser-executable="$HSHELL"
ls -la /tmp/v_grad.png /tmp/v_anim.png
```
然后用 Read 工具查看 `/tmp/v_grad.png`（应为 ktv 深蓝渐变 + 歌词）与 `/tmp/v_anim.png`（应为特效背景 + 歌词）。确认背景按预期渲染。

- [ ] **Step 13: 清理临时文件 + Commit**

```bash
cd /Users/dfbb/Sites/mtv/mutv/src && rm -f /tmp/bg_grad.json /tmp/bg_anim.json /tmp/v_grad.png /tmp/v_anim.png
cd /Users/dfbb/Sites/mtv/mutv
git add src/preset
git commit -m "refactor(preset): route all 8 presets through BackgroundLayer"
```

---

## Task 5: render.mjs — 互斥背景参数 + 资源处理 + props 注入

**Files:**
- Modify: `src/render.mjs`

- [ ] **Step 1: 改 import（第 29 行加 readFileSync 已有，确认 readFileSync/readdirSync 在列）**

`src/render.mjs` 第 29 行已是 `import {readFileSync, readdirSync, existsSync, copyFileSync, mkdirSync} from 'fs';`。无需改动（这些都已导入）。

- [ ] **Step 2: 替换背景图处理段（第 332-349 行）为三源互斥逻辑**

把 `// Handle background image` 到 `backgroundImage = bgName; }` 整段（第 332-349 行）替换为：
```js
// --- Background sources (mutually exclusive): video > image > anim ---
const bgFlags = ['bg-video', 'bg-img', 'bganim'].filter((k) => args[k]);
if (bgFlags.length > 1) {
  console.error(`Error: choose only one background source (got: ${bgFlags.map((f) => '--' + f).join(', ')})`);
  process.exit(1);
}

function copyToPublic(srcPath, kind) {
  const resolved = resolveFilePath(srcPath);
  if (!existsSync(resolved)) {
    console.error(`Error: ${kind} file not found: ${resolved}`);
    process.exit(1);
  }
  const pubDir = resolve('public');
  mkdirSync(pubDir, {recursive: true});
  const name = basename(resolved);
  const dest = resolve(pubDir, name);
  if (resolve(resolved) !== dest) {
    copyFileSync(resolved, dest);
    console.log(`Copied ${kind} to public/${name}`);
  }
  return name;
}

let backgroundImage = '';
let backgroundVideo = '';
let backgroundAnimHtml = '';

if (args['bg-img']) {
  backgroundImage = copyToPublic(args['bg-img'], 'background image');
} else if (args['bg-video']) {
  backgroundVideo = copyToPublic(args['bg-video'], 'background video');
} else if (args.bganim) {
  const animLabel = args.bganim;
  const animFile = resolve('animbg', animLabel, 'index.html');
  if (!existsSync(animFile)) {
    const avail = existsSync(resolve('animbg'))
      ? readdirSync(resolve('animbg')).filter((d) => existsSync(join(resolve('animbg'), d, 'index.html')))
      : [];
    console.error(`Error: bganim "${animLabel}" not found (expected ${animFile})`);
    console.error(`Available animated backgrounds: ${avail.length ? avail.join(', ') : '(none — run scripts/fetch_animbg.py)'}`);
    process.exit(1);
  }
  backgroundAnimHtml = readFileSync(animFile, 'utf-8');
  console.log(`Using animated background: ${animLabel}`);
}
```

- [ ] **Step 3: 把新字段加进 inputProps（第 372-384 行的对象）**

在 `inputProps` 对象里，`backgroundImage,` 一行之后加：
```js
  backgroundVideo,
  backgroundAnimHtml,
```

- [ ] **Step 4: 更新渲染日志（可选但有用）**

在 render 分支里 `if (backgroundImage) console.log(...)` 那行（约第 451 行）之后加：
```js
if (backgroundVideo) console.log(`  Background video: ${backgroundVideo}`);
if (backgroundAnimHtml) console.log(`  Background anim: ${args.bganim}`);
```
在 --html 分支里 `if (backgroundImage) console.log(...)`（约第 404 行）之后加同样两行。

- [ ] **Step 5: 更新 render.mjs 头部文档块（第 21 行）**

把第 21 行 `*   --background   Background image file path (if omitted, uses animated gradient)` 替换为：
```
 *   --bg-img       Background image file (mutually exclusive with --bg-video/--bganim)
 *   --bg-video     Background video file (mutually exclusive)
 *   --bganim       Animated background effect label under animbg/<label>/ (mutually exclusive)
```

- [ ] **Step 6: 语法自检**

Run: `cd /Users/dfbb/Sites/mtv/mutv/src && node --check render.mjs`
Expected: 无输出（语法正确）。

- [ ] **Step 7: 端到端：渲染一小段视频确认 bganim 注入贯通**

Run:
```bash
cd /Users/dfbb/Sites/mtv/mutv/src
LABEL=$(node -e "console.log(require('./animbg/manifest.json')[0].label)")
node cli.mjs --audio ../example/cn-1.mp3 --lyrics ../example/cn-1.srt --title "anim test" --preset ktv --bganim "$LABEL" --res 1280x720 --output /tmp/animbg_test.mp4 2>&1 | tail -15
ls -la /tmp/animbg_test.mp4
```
Expected: 渲染日志含 `Using animated background: <label>` 和 `Background anim: <label>`，最终生成 `/tmp/animbg_test.mp4`。

- [ ] **Step 8: 互斥校验**

Run:
```bash
cd /Users/dfbb/Sites/mtv/mutv/src
node cli.mjs --audio ../example/cn-1.mp3 --title x --bg-img a.png --bg-video b.mp4 2>&1 | head -3
```
Expected: 打印 `Error: choose only one background source (got: --bg-video, --bg-img)` 之类并非零退出（注意此处错误来自 render.mjs，需 Task 6 让 cli.mjs 先转发参数；若 Task 6 未完成，此步可在 Task 6 后补验）。

- [ ] **Step 9: 清理 + Commit**

```bash
cd /Users/dfbb/Sites/mtv/mutv/src && rm -f /tmp/animbg_test.mp4
cd /Users/dfbb/Sites/mtv/mutv
git add src/render.mjs
git commit -m "feat(render): mutually-exclusive bg-img/bg-video/bganim sources"
```

---

## Task 6: cli.mjs — 转发新参数 + help 文档

**Files:**
- Modify: `src/cli.mjs`

- [ ] **Step 1: 替换 `--background` 转发行（第 107 行）**

把 `src/cli.mjs` 第 107 行 `if (opts.background) nodeArgs.push('--background', resolve(opts.background));` 替换为：
```js
if (opts['bg-img']) nodeArgs.push('--bg-img', resolve(opts['bg-img']));
if (opts['bg-video']) nodeArgs.push('--bg-video', resolve(opts['bg-video']));
if (opts.bganim) nodeArgs.push('--bganim', opts.bganim);
```
（注意：`bg-img`/`bg-video` 是文件路径，resolve 成绝对路径；`bganim` 是 label，不 resolve。render.mjs 的互斥检查会兜底，但 cli 原样转发即可。）

- [ ] **Step 2: 更新 --help 文档块（第 26 行）**

把第 26 行 `*   --background  Background image file path` 替换为：
```
 *   --bg-img      Background image file (mutually exclusive with --bg-video/--bganim)
 *   --bg-video    Background video file (mutually exclusive)
 *   --bganim      Animated background effect label (see src/animbg/, mutually exclusive)
```

- [ ] **Step 3: 语法自检**

Run: `cd /Users/dfbb/Sites/mtv/mutv/src && node --check cli.mjs`
Expected: 无输出。

- [ ] **Step 4: 验证 help 显示新参数**

Run: `cd /Users/dfbb/Sites/mtv/mutv/src && node cli.mjs --help | grep -E "bg-img|bg-video|bganim"`
Expected: 三行新参数都出现。

- [ ] **Step 5: 重跑 Task 5 Step 8 的互斥校验（现在 cli 已转发）**

Run:
```bash
cd /Users/dfbb/Sites/mtv/mutv/src
node cli.mjs --audio ../example/cn-1.mp3 --title x --bg-img ../example/bg.jpg --bganim foo 2>&1 | grep -i "only one background"
```
Expected: 命中互斥错误信息。

- [ ] **Step 6: Commit**

```bash
cd /Users/dfbb/Sites/mtv/mutv
git add src/cli.mjs
git commit -m "feat(cli): forward bg-img/bg-video/bganim, update help"
```

---

## Task 7: 更新 USAGE.md

**Files:**
- Modify: `USAGE.md`

- [ ] **Step 1: 更新示例（第 58 行）**

把 `USAGE.md` 第 58 行的 `--background bg.jpg` 改为 `--bg-img bg.jpg`。

- [ ] **Step 2: 更新参数表（第 122 行）**

把第 122 行 `| `--background <file>` | 无 | 背景图片路径。省略时使用动态渐变背景。自动复制到 `public/`。 |` 替换为三行：
```
| `--bg-img <file>` | 无 | 背景图片文件。与 `--bg-video`/`--bganim` 互斥。自动复制到 `public/`。 |
| `--bg-video <file>` | 无 | 背景视频文件（循环播放）。与其它背景源互斥。自动复制到 `public/`。 |
| `--bganim <label>` | 无 | 动画特效背景，对应 `src/animbg/<label>/`（由 `scripts/fetch_animbg.py` 抓取）。与其它背景源互斥。 |
```

- [ ] **Step 3: 在目录结构区补充 animbg 与 scripts（第 12-25 行的目录树）**

在 `out/` 行之前加：
```
    BackgroundLayer.tsx  所有 preset 共用：按优先级渲染 video/img/anim/渐变背景
  animbg/              抓取的全屏动画特效（<label>/index.html + manifest.json）
  scripts/             fetch_animbg.py 抓取脚本 + requirements.txt
```

- [ ] **Step 4: 提交**

```bash
cd /Users/dfbb/Sites/mtv/mutv
git add USAGE.md
git commit -m "docs(usage): document bg-img/bg-video/bganim and animbg/"
```

---

## 完成标准

- POC 通过（iframe 动画逐帧生效），否则方案已与用户重新对齐。
- `python3 scripts/fetch_animbg.py` 跑通，`animbg/manifest.json` 有 40+ 条目，各 `index.html` 为完整文档。
- `npx tsc --noEmit` 退出码 0。
- 三类背景源 + 渐变兜底各渲一帧/一小段人工核验通过。
- `--bg-img` / `--bg-video` / `--bganim` 互斥校验生效；`--help` 显示三参数。
- `git push` 后本地与远程 main 一致（push 由用户确认时机）。



